import Slide from '../Slide';

const slide =  Slide (`
# Redux has many similar concepts as React

* Immutable Store
* One way data flow

This is why they fit so well together.
`);     

export default slide;